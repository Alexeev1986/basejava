package urise.webapp.storage.junit;

import com.urise.webapp.storage.MapEntryStorage;

class MapEntryStorageTest extends AbstractStorageTest {

    protected MapEntryStorageTest() {
        super(new MapEntryStorage());
    }
}